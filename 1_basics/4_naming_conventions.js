/**
 * Naming conventions
 * 
 * 1. 영어를 사용하여 문자와 숫자를 모두 사용 가능
 * 2. 특수 기호는 underscore and $ 사용가능
 * 3. 숫자로 이름을 시작 할 수 없다.
 *    1name, 2hello
 * 4. 키워드는 변수명으로 사용 할 수 없다.
 *    var var = 'var';
 */

let $ive = 'ive';
const _ive = 'ive';


/**
 * Naming convention 2
 * 1. camelCase
 * 2. snake_case
 * 3. PascalCase - C# ms 계열의 언어에서 사용
 * 
 */